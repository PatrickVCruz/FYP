<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.cruz.fyp.virtualassistant">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name="com.cruz.fyp.virtualassistant.AppController"
        android:allowBackup="true"
        android:icon="@mipmap/virtual_assistant_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/virtual_assistant_app_icon_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        android:configChanges="orientation"
        android:screenOrientation="portrait">
        <activity android:name=".GUI.RegisterActivity" />
        <activity android:name=".GUI.LoginActivity"/>
        <activity android:name=".MainActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    </application>

</manifest>